name: Setup Node
description: Setup Node 22 and install dependencies (repo must be checked out first)

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: npm
    - run: npm ci
      shell: bash
